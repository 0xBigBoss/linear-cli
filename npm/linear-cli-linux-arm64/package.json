{
  "name": "@0xbigboss/linear-cli-linux-arm64",
  "version": "0.2.11",
  "os": [
    "linux"
  ],
  "cpu": [
    "arm64"
  ],
  "scripts": {
    "prepublishOnly": "test -f linear || (echo 'Error: binary not found. Run build first.' && exit 1)"
  }
}
